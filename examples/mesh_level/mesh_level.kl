// GD32VW553 BLE Mesh Gen Level + vendor via klin-lang/gd32v_ble @v0.12.0.
// mesh_enable node includes OnOff + Level + vendor button (cid 0x05F1).
//   make emit KLIN=/path/to/klin/bin/klin.dart
import "../../gd32v_ble" ble

fn main() {
    let mut e = ble.init()
    if e != ble.err_ok() {
        return
    }
    e = ble.mesh_enable()
    if e != ble.err_ok() {
        return
    }

    while true {
        if ble.mesh_level_changed() {
            let _l = ble.mesh_level()
        }
        if ble.mesh_vnd_changed() {
            let _v = ble.mesh_vnd()
        }
        if ble.mesh_onoff_changed() {
            let _o = ble.mesh_onoff()
        }
        let mut i: i32 = 0
        while i < 20000000 {
            i = i + 1
        }
    }
}
